# Copy this file to .env and fill in real values.
# .env is gitignored - never commit real secrets.

# CORS allow-lists (dev defaults for local storefronts + admin dashboard).
# The three storefronts dev on ports 3000 (tiem-tao), 3001 (remart), 3002 (mot);
# list both localhost and 127.0.0.1 since the dev servers bind 127.0.0.1.
STORE_CORS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001,http://localhost:3002,http://127.0.0.1:3002
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:9000,http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001,http://localhost:3002,http://127.0.0.1:3002

# Postgres (local dev, no docker). Matches brew postgresql@17 on localhost.
# Use your local Postgres role in place of USER.
DATABASE_URL=postgres://USER@localhost:5432/vn_storefronts

# Redis (local dev, brew redis on localhost). Used by the event bus and
# workflow engine modules.
REDIS_URL=redis://localhost:6379

# Secrets. Generate strong random values for any non-local environment.
JWT_SECRET=change-me
COOKIE_SECRET=change-me

# Admin multi-factor encryption key. 32-byte hex: openssl rand -hex 32
AUTH_MFA_ENCRYPTION_KEY=change-me

# Onboarding flow shown by the admin dashboard.
MEDUSA_ADMIN_ONBOARDING_TYPE=default

# Stripe secret key for the INTL region (test mode). Get a test key from the
# Stripe dashboard (starts with sk_test_). Without it, the Stripe provider is
# wired but cannot process real payments (Stripe payment E2E is blocked).
STRIPE_API_KEY=
# Stripe webhook signing secret (starts with whsec_). Needed for webhook-driven
# order updates (3D Secure, async capture). Optional for basic test-mode charges.
STRIPE_WEBHOOK_SECRET=
